alias PPATH="echo $PATH | tr --squeeze-repeats \":\" \"\n\""
alias ls="ls --color=auto --group-directories-first --classify"

load_if_exists "$HOME/.config/dotfile/aliases.local"
load_if_exists "$HOME/.config/dotfile/aliases.secret"
