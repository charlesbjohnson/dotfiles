# fmt: shfmt

dotfile::env::direnv

dotfile::set_env GOPATH "$HOME/.go"
dotfile::prepend_env PATH "$GOPATH/bin"

dotfile::set_env CARGO_HOME "$HOME/.cargo"
dotfile::prepend_env PATH "$CARGO_HOME/bin"
