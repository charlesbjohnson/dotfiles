# fmt: shfmt

# direnv
eval "$(direnv hook "$(basename "$SHELL")")"

# go
export GOPATH="$HOME/.go"
dotfile::prepend_env PATH "$GOPATH/bin"

# rust
export CARGO_HOME="$HOME/.cargo"
dotfile::prepend_env PATH "$CARGO_HOME/bin"
